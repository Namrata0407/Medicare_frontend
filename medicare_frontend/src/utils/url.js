const bpMonitorURL = `${process.env.REACT_APP_BASEURL}/homeProducts?category=bp_monitors`;
const popularComboDealsURL = `${process.env.REACT_APP_BASEURL}/homeProducts?category=popular_combo_deals`;

export { bpMonitorURL, popularComboDealsURL };
